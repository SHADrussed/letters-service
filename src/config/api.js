// Указываем сдесь URL API
const API_URL = import.meta.env.VITE_API_URL;
export { API_URL };

// Затестить без URL / If no url - true
export const USE_MOCK = false;

// Mock error
export const TEST_ERROR = false;
